const numeros = [10,20,30,40,50]

const copiaNumeros = [...numeros] // crio uma cópia sem mexer no original

// const copiaNumeros = numeros cópia alocada no mesmo espaço de memória, altera o original